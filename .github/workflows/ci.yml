name: C++ CI
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

concurrency:
  group: cpp-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  build:
    strategy:
      matrix:
        runner: [ubuntu-24.04, ubuntu-24.04-arm]
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v4
      - name: Install production database client libraries
        run: |
          sudo apt-get update
          sudo apt-get install -y libsqlite3-dev libpq-dev libmariadb-dev
      - name: Configure release (warnings as errors)
        run: cmake --preset release -DOGL_ENABLE_WERROR=ON
      - name: Build
        run: cmake --build --preset release
      - name: Unit tests
        run: ctest --preset release
      - name: Integrated world/social/handoff smoke test
        run: bash ./scripts/run-smoke.sh
      - name: Script World query and ACK smoke test
        run: bash ./scripts/smoke-script-world.sh
      - name: OGL and LSL ScriptEngine smoke test
        run: bash ./scripts/smoke-script-engine.sh
      - name: Crossing v3 reserve and rollback smoke test
        run: bash ./scripts/smoke-crossing-v3.sh
      - name: Object Crossing end-to-end smoke test
        run: bash ./scripts/smoke-object-crossing.sh
